<h2>Grupos</h2>

<a href="/admin/add-group" class="btn btn-success" style="margin:5px 0 15px 0;"><i class="icon-plus icon-white"></i> Nuevo Grupo</a>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Nombre</th>
            <th>Acciones</th>
        </tr>
    </thead>
    <tbody>
        <?php foreach ($this->groups as $group): ?>
        <tr>
            <td><?php echo $group->getName(); ?></td>
            <td>
                <div class="btn-group">
                  <a class="btn btn-primary dropdown-toggle" data-toggle="dropdown" href="#">
                    <i class="icon-tasks icon-white"></i>
                    <span class="caret"></span>
                  </a>
                  <ul class="dropdown-menu">
                    <li><a href="/admin/edit-group?groupId=<?php echo $group->getId(); ?>"><i class="icon-pencil"></i> Editar</a></li>
                    <li><a href="/admin/delete-group?groupId=<?php echo $group->getId(); ?>" onclick="return confirm('Are you sure you want to delete this group?')"><i class="icon-trash"></i> Borrar</a></li>
                  </ul>
                </div>
            </td>
        </tr>
        <?php endforeach ?>
    </tbody>
</table>